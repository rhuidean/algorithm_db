from tries import *

def insert(self,str):
	if str[0] not in (self.root.children):
		key = TrieNode()
		str[0]
		self.rood.children.append(str[0])
		str = str[1:]
		for index in range(len(str)):

			if index =
			str.pop(0)=TrieNode()

			print 1

Triset.insert = insert

trisetA = Triset()
print trisetA.insert("King")